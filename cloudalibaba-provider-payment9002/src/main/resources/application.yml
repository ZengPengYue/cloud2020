server:
  port: 9002

spring:
  application:
    name: nacos-payment-provider
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848 #配置nacos地址
        # 换成nginx的1111端口，做集群
#        server-addr: 120.78.129.173:1111
management:
  endpoints:
    web:
      exposure:
        include: '*'